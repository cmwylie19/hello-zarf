apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zarf-practice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "zarf-practice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zarf-practice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
